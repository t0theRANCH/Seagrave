<PictureListView>:
    name: "picture_list_view"
    id: picture_list_view

    MDBoxLayout:
        size_hint: 1, 1
        orientation: 'vertical'
        ScrollView:
            size_hint: 1, 1
            MDGridLayout:
                id: container
                cols: 2
                adaptive_height: True
                padding: dp(4), dp(4)
                spacing: dp(4)
                row_default_height: (self.width - self.cols*self.spacing[0]) / self.cols
                row_force_default: True
                col_default_width: (self.width - self.cols*self.spacing[0]) / self.cols
                col_force_default: True
        AnchorLayout:
            anchor_x: 'right'
            anchor_y: 'bottom'
            padding: [dp(10), dp(10), dp(20), dp(40)]
            MDFloatingActionButton:
                icon: "arrow-left-circle"
                on_release: root.controller.go_back()